import React, {FC} from 'react';
import styles from './patches.module.scss'

const Patches: FC = () => {
    return (
        <div>
            Patches
        </div>
    );
};

export default Patches;